package app;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Statement;

/* Class for managing the JDBC Connection to a MySQL database */
public class jdbcConnection {
    private static final String DATABASE = ""; // TODO: Add database file name

    /* Add methods for getting data for each function / web-page - should run queries etc. */
    // TODO:

}